// Logout.js
import React from 'react';
import { Button } from 'antd';

const Logout = () => {
  return (
    <Button type="default" style={{ marginRight: '30px' }}>Logout</Button>
  );
};

export default Logout;