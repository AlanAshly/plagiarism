import React from 'react';
import { Button } from 'antd';

const Profile = () => {
  return (
    <Button type="default" style={{ marginRight: '10px' }}>Profile</Button>
  );
};

export default Profile;